export type AccountEntity = {
  accountId: string;
  username: string;
  password: string;
  sessionCookie: string;
  accessToken: string;
}

export const createAccount = () => {
  
}

export const updateAccount = () => {

}

